meta {
  name: Excluir Reserva (com Transação)
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/reservas/1
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

vars:pre-request {
  baseUrl: http://localhost:3000
  token: cole-o-token-do-login
}
