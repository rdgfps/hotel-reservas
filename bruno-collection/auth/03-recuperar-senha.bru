meta {
  name: Recuperar Senha
  type: http
  seq: 3
}

post {
  url: {{baseUrl}}/auth/recuperar-senha
  body: json
  auth: none
}

body:json {
  {
    "email": "admin@hotel.com"
  }
}

vars:pre-request {
  baseUrl: http://localhost:3000
}
